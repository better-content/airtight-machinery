# Airtight Machinery

Adds an `airtight_machinery:airtight_upgrade` item that seals Create Basins and PneumaticCraft Fluid Mixers and Thermopneumatic Processing Plants against Latent ChemLib gas escape. Apply the upgrade by using it on a supported machine; sneak-use it empty-handed to remove it.

Requires Latent ChemLib, Heat Sync, ChemLib, Create, and PneumaticCraft. The pack recipe and teaching annotation are maintained by the modpack.

Build with `./gradlew verifyFast`, run runtime and GameTest validation with `./gradlew verifyFull`, and stage the reobfuscated jar with `./gradlew stageRuntimeJar`.
